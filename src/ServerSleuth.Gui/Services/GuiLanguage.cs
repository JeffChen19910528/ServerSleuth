namespace ServerSleuth.Gui.Services;

/// <summary>The two UI languages this GUI supports. Default is <see cref="English"/> — unchanged
/// behavior for anyone already using the app before this toggle existed.</summary>
public enum GuiLanguage
{
    English,
    TraditionalChinese
}
